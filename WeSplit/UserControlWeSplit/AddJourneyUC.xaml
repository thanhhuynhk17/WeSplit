<UserControl x:Class="WeSplit.UserControlWeSplit.AddJourneyUC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WeSplit.UserControlWeSplit"
             
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
             
             TextElement.Foreground="{DynamicResource MaterialDesignBody}"
            TextElement.FontWeight="Regular"
            TextElement.FontSize="13"
            TextOptions.TextFormattingMode="Ideal"
            TextOptions.TextRenderingMode="Auto"
            Background="{DynamicResource MaterialDesignPaper}"
            FontFamily="{DynamicResource MaterialDesignFont}"
             
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <materialDesign:Card materialDesign:ShadowAssist.ShadowDepth="Depth5"
                                 Padding="10" Margin="10">

            <StackPanel Width="800" Height="450" Orientation="Horizontal">
                <StackPanel Width="400">
                    <TextBlock Margin="0 5 0 0" Text="Tên chuyến đi"></TextBlock>
                    <TextBox Margin="0 0 0 15" materialDesign:HintAssist.Hint="Tên chuyến đi" Style="{StaticResource MaterialDesignFloatingHintTextBox}" />
                    <TextBlock Margin="0 5 0 0" Text="Điểm đến"></TextBlock>
                    <StackPanel>
                        <ComboBox ItemsSource="{Binding ListPlace}" materialDesign:ComboBoxAssist.ShowSelectedItem="{Binding ElementName=PlaceSelected}"
                            Margin="0 10 0 15" Width="200" HorizontalAlignment="Left" VerticalAlignment="Bottom" >
                        </ComboBox>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <DatePicker Margin="0 0 80 0" Width="100" materialDesign:HintAssist.Hint="Ngày đi"
                            Style="{StaticResource MaterialDesignFloatingHintDatePicker}" />
                        <DatePicker Width="100" materialDesign:HintAssist.Hint="Ngày về"
                            Style="{StaticResource MaterialDesignFloatingHintDatePicker}" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0 15 0 0">
                        <CheckBox x:Name="HireCarCheckbox" Margin="0 0 10 0">
                           Thuê xe
                        </CheckBox>
                        <CheckBox x:Name="HireRoomCheckbox" Margin="0 0 10 0">
                            Thuê phòng
                        </CheckBox>
                        <CheckBox x:Name="HireFlaneCheckbox" Margin="0 0 10 0">
                            Máy bay
                        </CheckBox>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBox Width="70" Margin="0 0 0 15" materialDesign:HintAssist.Hint="Chi phí" Style="{StaticResource MaterialDesignFloatingHintTextBox}" />
                        <TextBox Width="70" Margin="0 0 0 15" materialDesign:HintAssist.Hint="Chi phí" Style="{StaticResource MaterialDesignFloatingHintTextBox}" />
                        <TextBox  Margin="0 0 0 15" materialDesign:HintAssist.Hint="Chi phí" Style="{StaticResource MaterialDesignFloatingHintTextBox}" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal"></StackPanel>
                </StackPanel>
                <StackPanel Width="400"></StackPanel>
            </StackPanel>
        </materialDesign:Card>
    </Grid>
</UserControl>
